# v1.0.1 Plan

> **Release version:** `1.0.1`
> **Development version:** `1.0.1-dev`
> **Status:** Active
> **Theme:** Post-v1.0 stabilisation

## Outcome

Confirms that the v1.0.0 GA baseline (docstring coverage, Standard-001
nomenclature, caching/parallel telemetry) is still clean after the release
planning and tooling simplification below, then ships `1.0.1`. No new runtime
behavior for users; maintainers get a release-planning surface with a single
source of truth per concern (GitHub issues for proposed work, GitHub
milestones for approved scope, this file for active-release coordination) and
release automation that no longer parses or rewrites roadmap prose.

## Included work

| ID | Deliverable | Issue | Governing ADR/standard | Required evidence | Status |
|---|---|---|---|---|---|
| T1 | Simplify release planning: delete `development/current-work/RELEASE_PLAN.md`, stop release automation from parsing/rewriting it, rewrite this plan and its template to a compact format, update release-related skills and navigation docs | — | — | `pytest tests/scripts/test_local_checks_release_workflow.py -q`; `python scripts/quality/check_version_alignment.py --check --allow-normalized`; `make capability-chain-check`; `make local-checks-pr` | Done |
| T2 | Re-run the Standard-002 docstring coverage gate and the Standard-001 nomenclature gate on the v1.0.1 candidate commit; compare against the v1.0.0 GA baseline (96.64% coverage, zero nomenclature violations) | — | STD-001, STD-002 | `python scripts/quality/check_docstring_coverage.py --fail-under 94.0`; `python scripts/quality/check_std001_nomenclature.py --root src/calibrated_explanations --check` | Not started |
| T3 | Re-run the caching/parallel staging-equivalent smoke from `development/finished-work/v1.0.0-rc_plan.md` §3 and compare telemetry to the RC baseline (cache `hits=1, misses=1`; parallel `strategy="threads", workers=2, worker_utilisation_pct=100.0`) | — | ADR-003, ADR-004 | `CE_CACHE=1 pytest tests/unit/cache tests/unit/perf/test_cache.py -v`; `CE_PARALLEL="enable,threads,workers=2,min_batch=1,min_instances=1,tiny=1" pytest tests/unit/core/test_parallel_benchmark.py tests/unit/core/test_parallel_deprecation.py tests/unit/core/test_parallel_helpers.py -v` | Not started |
| T4 | Publish `1.0.1` following `release.md` | — | — | `make release-preflight`; `make release-finalize`; maintainer steps 11-13; `make release-postcommit` | Not started |

## Excluded

- Any new public API, plugin category, or runtime behavior change.
- Caching/parallel on-by-default graduation (stays opt-in per ADR-003/ADR-004
  pending a clean telemetry sweep under T3).
- `auto_encode='auto'` wrapper preprocessing, `transform_to_numeric`
  deprecation, ranking-semantics consolidation, and plugin-author diagnostics
  — none are approved for this milestone; file or link a GitHub issue before
  scheduling any of them into a future milestone.

## Dependencies

- T4 (release publication) runs only after T1-T3 are closed or explicitly
  deferred with maintainer approval.

## Release-specific gates

- Docstring coverage and Standard-001 nomenclature checks pass on the release
  commit (T2).
- Caching/parallel telemetry shows no regression against the RC/GA baseline
  (T3).
- Standard repository gates (tests, lint, docs build, version alignment) are
  owned by `release.md` and `make release-preflight`; not repeated here.

## Release decision

`Not ready` — T2, T3, and T4 have not started.

T1 evidence (2026-07-22): `pytest tests/scripts/test_local_checks_release_workflow.py -q`
→ 33 passed; `make capability-chain-check` → 0 errors; `make local-checks-pr`
→ 4022 passed, 1 skipped, pre-commit all green; `python -m sphinx -W
--keep-going docs docs/_build/html` → build succeeded; `python -m sphinx -b
linkcheck docs docs/_build/linkcheck` → build succeeded (only pre-existing
external DOI redirects, no broken links).
