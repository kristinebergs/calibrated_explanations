# v1.0.0 Release Task Implementation Plan

> **Release version:** `1.0.0`
> **Development version:** `1.0.0-dev`

> **Baselined 2026-07-15**, immediately after `v1.0.0rc1` shipped (tag
> `v1.0.0rc1`, commit `0464593aeb0a0e89660027e0c94b39ece8142448`, PyPI
> verified by `make release-postcommit`). Plan archived at
> `development/finished-work/v1.0.0-rc_plan.md`.

> **GA BLOCKED pending `v1.0.0rc2` (recorded 2026-07-20):** a release-blocking
> plot-plugin dispatch defect was found after RC1: the public factual,
> alternative, and global/dashboard plotting APIs did not preserve the plugin
> request end to end, forcing third-party visualization packages to
> monkey-patch CE plotting callables. Under this plan's own emergency-patch
> rule, the **"no planned implementation" posture now has exactly one
> explicitly approved emergency compatibility exception**: the CE-native
> third-party plot dispatch fix tracked in
> `development/current-work/v1.0.0-rc2_plan.md`. Consequences:
>
> - **`v1.0.0rc2` — not `v1.0.0rc1` — becomes the final GA baseline.**
> - Unaffected RC1 evidence (schema freeze, caching/parallel staging
>   sign-off, deprecation closure, docstring coverage) carries forward.
> - Plugin-dispatch evidence and the public API snapshots (additive
>   `PlotRenderContext.runtime`, `PluginManager.resolve_plot_plugin_strict`)
>   **must be revalidated** on the RC2 candidate before GA.
> - `Development Status :: 5 - Production/Stable` promotion remains blocked
>   until `v1.0.0rc2` is published and verified.

> **GA posture:** v1.0.0 is a **stability-declaration milestone**, not an
> implementation milestone. Every ADR and Standard in scope for v1.0.0 was
> closed before or during the RC (see `RELEASE_PLAN_v1.md` §"ADR and
> Standards roadmap summary": all rows Completed or explicitly deferred
> post-v1.0). This plan's tasks are confirmation, announcement, and the
> mechanical GA cutover (classifier promotion, tag, publish) — not new
> feature or remediation work. If a release-blocking defect is found, treat
> it as an emergency patch (same discipline as the RC), not a planned task.

This plan expands the `v1.0.0 (stability declaration)` milestone from
`development/current-work/RELEASE_PLAN_v1.md` into actionable steps.

## Source references reviewed

- `development/current-work/RELEASE_PLAN_v1.md` — `### v1.0.0 (stability declaration)` section (the authoritative task list and release gate) and the "ADR and Standards roadmap summary" (confirms no open appendix gaps target v1.0.0 GA)
- `development/current-work/RELEASE_PLAN_status_appendix.md` — per-ADR gap rows; confirmed no row targets v1.0.0 GA
- `development/finished-work/v1.0.0-rc_plan.md` — RC validation evidence this milestone carries forward (Section 9 release evidence, Tasks 0–7)
- `release.md` — authoritative 17-step maintainer sequence
- `docs/upgrade/v1.0.0-upgrade-checklist.md` — upgrade checklist to circulate at GA
- `docs/schema_v1.md` — schema freeze statement (already published at RC)
- `pyproject.toml` — current `Development Status :: 4 - Beta` classifier

## Release tasks covered (from RELEASE_PLAN_v1.md)

The master-plan items below keep `RELEASE_PLAN_v1.md`'s original numbering;
this plan's tasks are numbered in **execution order**, so the mapping is
noted per item.

1. Announce the stable plugin/telemetry contracts and publish the final compatibility statement across README, CHANGELOG, and docs hub. → plan Task 3
2. Promote packaging metadata from `Development Status :: 4 - Beta` to `Development Status :: 5 - Production/Stable`, only after all v1.0.0 release gates are closed and release-blocking defects are zero. → plan Task 5
3. Confirm that staging validation signed off at v1.0.0-rc remains valid, and confirm zero active deprecations. → plan Task 1
4. Confirm Standard-001/Standard-002 guardrails remain enforced post-tag, monitor caching/parallel telemetry dashboards, and schedule maintenance cadences for the first patch release. → plan Task 2
5. Verify documentation hubs are current, navigable, and reflect v1.0.0 API state. → plan Task 4
6. Tag the v1.0.0 release and circulate the upgrade checklist to partners with caching and parallelisation guidance (folded into Task 6, Release preparation, since tagging is release.md steps 11–13). → plan Task 6

## Global rules

- **Tasks are numbered in execution order.** Execute Task 1 → Task 6 in sequence: the confirmation tasks (1–2) fail fast on evidence the announcement (Task 3) cites as still valid; the docs verification (Task 4) validates all content edits; the classifier promotion (Task 5) is gated on Tasks 1–4; Release preparation (Task 6) is always last.
- **No planned implementation.** Same discipline as the RC: every task below is confirmation, announcement, or the mechanical GA cutover. A release-blocking defect is an emergency patch (separate change, full gate re-run), not a scope addition to this plan.
- **RC evidence carries forward.** Caching/parallel staging validation, the deprecation-closure gate, docstring coverage, and the schema freeze statement were all verified at RC (`development/finished-work/v1.0.0-rc_plan.md` Section 9). GA does not re-run staging validation unless a post-RC release-blocking defect was patched (per the RC→GA release gate in `RELEASE_PLAN_v1.md`).
- **Classifier promotion is GA-gated.** `Development Status :: 5 - Production/Stable` must not be set until every other task in this plan except Release preparation (i.e., Tasks 1–4) is closed.

---

## 1) Staging validation carry-forward and deprecation ledger re-confirmation

### 1.0 Goal

Confirm the caching/parallel staging validation signed off at RC
(`v1.0.0-rc_plan.md` Task 3) remains valid without re-running new staging
tests, and re-confirm the deprecation ledger is still empty on the GA
candidate commit.

### 1.1 Status assessment

Not started (confirmation only; underlying validation already complete at
RC — see `development/finished-work/v1.0.0-rc_plan.md` Section 3.7 and
Section 9).

### 1.2 Relevant references

- `development/finished-work/v1.0.0-rc_plan.md` Section 3 (caching/parallel staging sign-off) and Section 0.1 (deprecation-closure gate validation)
- `docs/migration/deprecations.md` — must still show zero active entries
- ADR-003, ADR-004 — caching/parallel contracts being carried forward unchanged

### 1.3 Current anchors in code/docs

- `docs/foundations/how-to/safe-defaults.md` — telemetry/toggle documentation from RC, unchanged
- `make deprecation-closure` — the corrected gate from v0.11.6

### 1.4 Gaps

- No new gaps; this is a re-confirmation task. The only risk is drift if an emergency patch landed between RC tag and GA candidate commit.

### 1.5 Implementation steps

1. Diff `git log v1.0.0rc1..HEAD` for any changes to `src/calibrated_explanations/cache/`, `src/calibrated_explanations/parallel/`, or their tests. If none, record "no changes since RC; staging validation carries forward unchanged."
2. If changes exist, determine whether any is a release-blocking defect patch touching caching/parallel; if so, re-run the RC Task 3 staging smoke and record new evidence.
3. Re-run `make deprecation-closure` on the GA candidate commit; confirm `active_rows_count: 0`, `blocking_rows_count: 0`.
4. Confirm `docs/migration/deprecations.md` "Active deprecations" section is still empty.

### 1.6 Verification checklist

- [ ] `git log v1.0.0rc1..HEAD` reviewed for cache/parallel changes; none found, or re-validated if found
- [ ] `make deprecation-closure` exits 0 on the GA candidate commit; zero active/blocking rows
- [ ] `docs/migration/deprecations.md` has zero active entries
- [ ] `make local-checks-task TASK=1` exits 0

---

## 2) Standard-001/Standard-002 guardrails and post-release maintenance cadence

### 2.0 Goal

Confirm nomenclature (Standard-001) and docstring-coverage (Standard-002)
guardrails remain enforced post-tag, and schedule the first-patch-release
maintenance cadence (coverage/docstring audits, performance regression
sweeps).

### 2.1 Status assessment

Not started.

### 2.2 Relevant references

- `development/standards/STD-001-nomenclature-standardization.md`
- `development/standards/STD-002-code-documentation-standard.md`
- `development/finished-work/v1.0.0-rc_plan.md` Section 4 (96.62% docstring coverage at RC)
- `scripts/quality/check_docstring_coverage.py`, `scripts/quality/check_std001_nomenclature.py`

### 2.3 Current anchors in code/docs

- `make local-checks` / `make release-preflight`'s `release` profile — both run the STD-001/STD-002 gates on every release

### 2.4 Gaps

- No implementation gap; this task is a confirmation plus a one-time cadence-scheduling decision (where/how the first patch-release maintenance checkpoint is tracked) that doesn't currently exist anywhere.

### 2.5 Implementation steps

1. Re-run `python scripts/quality/check_docstring_coverage.py --fail-under 94.0` and `python scripts/quality/check_std001_nomenclature.py --root src/calibrated_explanations --check` on the GA candidate commit; record the actual coverage percentage.
2. Decide and record where the first-patch-release maintenance cadence is tracked (e.g., a dated note in `RELEASE_PLAN_v1.md`'s "Post-1.0 considerations" section, or a new `development/current-work/v1.0.1_plan.md` placeholder) — do not invent new tooling infrastructure for this.
3. Record the decision in this task's checklist with a dated note.

### 2.6 Verification checklist

- [ ] Docstring coverage ≥94% confirmed on the GA candidate commit; actual value recorded
- [ ] Standard-001 nomenclature gate passes with zero violations on the GA candidate commit
- [ ] First-patch-release maintenance cadence tracking location decided and recorded
- [ ] `make local-checks-task TASK=2` exits 0

---

## 3) Stable-contract announcement and compatibility statement

### 3.0 Goal

Publish the final v1.0.0 compatibility statement — the public plugin and
telemetry contracts are now stable — across README, CHANGELOG, and the docs
hub, superseding the RC's "frozen, patch-fixes-only" framing with a GA
statement.

### 3.1 Status assessment

Not started.

### 3.2 Relevant references

- `README.md` — RC compatibility note (Section 2 of the archived RC plan) to be updated to a GA statement
- `CHANGELOG.md` — RC entry's API-freeze wording to be superseded by a GA entry
- `docs/schema_v1.md` — existing schema freeze statement (no change needed; already GA-appropriate)
- ADR-006 (plugin trust model), ADR-037 (visualization extension governance) — contracts being declared stable

### 3.3 Current anchors in code/docs

- `README.md` "Public API" / compatibility section (added at RC)
- `CHANGELOG.md` `## [v1.0.0rc1]` entry's "Public API" bullet

### 3.4 Gaps

- README and CHANGELOG still speak in RC terms ("frozen as of `1.0.0rc1`", "only patch-level defect fixes will be accepted before GA"). Needs a GA-dated statement declaring the plugin/telemetry contracts stable for `1.0.0`.

### 3.5 Implementation steps

1. Update the README compatibility note to a GA statement: the public API, plugin trust model (ADR-006), and visualization extension contract (ADR-037) are stable as of `1.0.0`.
2. Draft the `1.0.0` CHANGELOG entry (added by `make release-preflight`'s `[Unreleased]` → dated-section conversion; author the prose ahead of time) with the GA compatibility statement, referencing the RC's frozen-schema and deprecation-closure evidence re-confirmed in Task 1 as still valid.
3. Confirm `docs/schema_v1.md`'s existing freeze statement needs no wording change (it already states v1 semantics are frozen, not RC-specific).

### 3.6 Verification checklist

- [ ] README compatibility section states the public API/plugin/telemetry contracts are stable as of `1.0.0`, not RC-frozen
- [ ] `CHANGELOG.md` `1.0.0` entry contains the GA compatibility statement
- [ ] `docs/schema_v1.md` freeze statement reviewed; no RC-specific wording remains
- [ ] `make local-checks-task TASK=3` exits 0

---

## 4) Documentation hubs current for v1.0.0 API state

### 4.0 Goal

Verify the practitioner, researcher, contributor, and get-started
documentation hubs are current, navigable, and reflect the v1.0.0 API
state (no stale RC-only language, no stale pre-v1 API references).

### 4.1 Status assessment

Not started (Task 5 of the RC plan already closed this for the RC build on
2026-07-15; this task re-confirms nothing regressed between RC and GA).

### 4.2 Relevant references

- `development/finished-work/v1.0.0-rc_plan.md` Section 5 (RC documentation review evidence)
- `docs/upgrade/v1.0.0-upgrade-checklist.md` — to be circulated to partners per master-plan item 6
- Standard-004 (documentation audience hubs)

### 4.3 Current anchors in code/docs

- `docs/` practitioner/researcher/contributor/get-started hubs
- `docs/conf.py` — dynamic `release` resolution from installed package metadata

### 4.4 Gaps

- Docs currently still carry RC-specific notices ("release candidate", "API frozen except release-blocking fixes", the RC install pin as primary) that need updating to GA language once the classifier promotes and the tag is `v1.0.0`.
- `scheduled.yml`'s `notebook-execution` job (`scripts/docs/run_notebooks.py`, advisory-with-artifact) completed with conclusion `cancelled` on the exact GA candidate commit (`0464593a`, run [29430586434](https://github.com/Moffran/calibrated_explanations/actions/runs/29430586434)), after an immediately prior run on the near-identical commit `16e2d2e7` passed cleanly. RC evidence (`v1.0.0-rc_plan.md` §9) recorded this as "runner-level cancellation, not a code defect" without independent confirmation. `release-preflight` step 5 (`release.md` step 5: "install the release tree and execute/save all governed release notebooks") is **blocking**, unlike this advisory CI job — if the same notebook hangs or is interrupted during the live preflight run, it stalls the release instead of surfacing early. This must be re-verified as a clean pass before relying on preflight's notebook lane, not carried forward as an assumption.

### 4.5 Implementation steps

1. Update the RC notice on the docs landing page to a GA notice (remove "release candidate" language; state the API is stable as of `1.0.0`).
2. Update the install instructions (README and docs) primary pin from `calibrated-explanations==1.0.0rc1` to `calibrated-explanations==1.0.0` (or unpinned `pip install calibrated-explanations` as primary now that GA is out, per normal post-GA convention — decide and record which).
3. Re-run `sphinx-build -b linkcheck docs docs/_build/linkcheck` (also covered by `make release-preflight`'s `release` profile) and confirm zero broken links. Run this after Task 3's README/CHANGELOG edits so the check covers all GA content changes.
4. Re-run the `scheduled.yml` `notebook-execution` job (`workflow_dispatch`) on the GA candidate commit, or run `python scripts/docs/run_notebooks.py --mode advisory --cell-timeout 30 --notebook-timeout 300` locally, to obtain an independent, non-cancelled result before Task 6 starts `make release-preflight`. If it fails (not merely re-cancels) on a specific notebook, treat that as a release-blocking defect per the plan's global rules, not a Task 4 scope addition.
5. Circulate `docs/upgrade/v1.0.0-upgrade-checklist.md` to partners (record how — e.g., linked from the GitHub release notes and CHANGELOG).

### 4.6 Verification checklist

- [ ] Docs landing page GA notice present; no stale "release candidate" wording remains
- [ ] Install instructions updated for GA (exact pin or unpinned primary decided and applied consistently in README and docs)
- [ ] `sphinx-build -b linkcheck` zero broken links on the GA candidate commit
- [ ] Notebook execution re-verified clean (non-cancelled) on the GA candidate commit, independent of the RC's unconfirmed "runner flake" assumption
- [ ] Upgrade checklist circulation method recorded
- [ ] `make local-checks-task TASK=4` exits 0

---

## 5) Packaging classifier promotion to Production/Stable

### 5.0 Goal

Promote `pyproject.toml`'s `Development Status` classifier from
`4 - Beta` to `5 - Production/Stable`, gated on every other GA task being
closed first (per `RELEASE_PLAN_v1.md`'s packaging metadata maturity
control).

### 5.1 Status assessment

Not started. Current classifier is `Development Status :: 4 - Beta`
(`pyproject.toml` line 27).

### 5.2 Relevant references

- `RELEASE_PLAN_v1.md` §"Packaging metadata maturity control" — promotion gate conditions
- `pyproject.toml` classifiers block

### 5.3 Current anchors in code/docs

- `pyproject.toml`: `"Development Status :: 4 - Beta"`

### 5.4 Gaps

- Classifier not yet promoted; must not be edited until Tasks 1–4 are closed (this task is deliberately last before Release preparation).

### 5.5 Implementation steps

1. Confirm all of: zero active deprecations (Task 1), CI gates green (Task 2), public API contract frozen (Task 3), docs/examples current (Task 4), no unresolved ADR/Standard contradiction (source references above), no open high-severity runtime bug, release artifacts built and installation-smoke-tested (will be re-confirmed by `make release-preflight`).
2. Edit `pyproject.toml`: change `"Development Status :: 4 - Beta"` to `"Development Status :: 5 - Production/Stable"`.
3. Confirm `Development Status :: 6 - Mature` is not used (explicitly out of scope for v1.0.0 per `RELEASE_PLAN_v1.md`).

### 5.6 Verification checklist

- [ ] All GA gate preconditions (Tasks 1–4) closed before this edit
- [ ] `pyproject.toml` classifiers contain `Development Status :: 5 - Production/Stable` and no `4 - Beta` / `3 - Alpha` / `6 - Mature` entries
- [ ] Built wheel/sdist metadata (via `make release-preflight`) reflects the promoted classifier
- [ ] `make local-checks-task TASK=5` exits 0

---

## 6) Release preparation

### 6.0 Goal

Complete the full `release.md` 17-step sequence for `1.0.0`. Repository
automation owns steps 1–10 and 14–17; the maintainer owns only the
immutable/external steps 11–13. This task is always last and executes only
after Tasks 1–5 are closed.

### 6.1 Status assessment

Not started.

### 6.2 Relevant references

- `release.md` — authoritative 17-step maintainer sequence
- `development/current-work/RELEASE_PLAN_v1.md` — milestone and next-version authority
- this version plan — exact release/development version declarations (`1.0.0` / `1.0.0-dev`)
- `Makefile` — `release-preflight`, `release-finalize`, `release-postcommit`
- `scripts/local_checks.py` — version-agnostic implementation and reports (including the content-hash-based `release-finalize` worktree check fixed during the RC)
- `.claude/skills/ce-release-planner/SKILL.md` — next-plan completion contract

### 6.3 Current anchors in code/docs

- `make release-preflight` owns steps 1–10: readiness, release-file/changelog preparation, tests, notebooks, alignment, clean build, Twine/artifact checks, and clean-wheel smoke.
- `make release-finalize` proves the preflight snapshot is still current (content-hash based; tolerates committing exactly what preflight produced before step 11).
- Steps 11–13 remain human-gated: commit/tag/push, RTD publication, PyPI upload.
- `make release-postcommit` owns steps 14–17: PyPI page/metadata verification, exact published-install smoke, plan handoff/archive, and next-development bump.

### 6.4 Gaps

All of Tasks 1–5 must be complete before preflight, including the classifier
promotion (Task 5) — `make release-preflight`'s release-file preparation
does not touch classifiers, so Task 5's `pyproject.toml` edit must land
before running preflight, not be expected to happen automatically.

### 6.5 Implementation steps

1. Confirm Tasks 1–5 are closed (readiness guard also enforces this).
2. Confirm this plan's exact release (`1.0.0`) and development (`1.0.0-dev`) version declarations match the milestone.
3. Run `make release-preflight` (release.md steps 1–10). Confirm its report lists `automated_release_steps: [1..10]`, the updated release files, and `preflight_passed: true`.
4. Run `make release-finalize` immediately before handoff.
5. Maintainer performs release.md steps 11–13 only: commit/tag/push `v1.0.0`, publish and verify RTD (set `v1.0.0` as the RTD **stable** version), upload the built artifacts to PyPI.
6. Run `make release-postcommit` (steps 14–17). Use `NEXT_VERSION=<milestone>` only if the master plan does not already name the intended next milestone (e.g. `1.0.1` or `1.1.0`, per whatever `RELEASE_PLAN_v1.md` names post-GA).
7. If postcommit scaffolds the next plan, immediately replace the placeholder with a complete plan through `ce-release-planner`; verify master tracking, released-plan archive, and development version are correct.
8. Archive `RELEASE_PLAN_v1.md` itself to `development/finished-work/` per its own "Active scope" note ("Moves to `finished-work` when v1.0.0 GA ships and the release series closes") — this is a manual step outside `make release-postcommit`'s automation, since that command only archives the version plan, not the master release-control document.

### 6.6 Verification checklist

- [ ] Tasks 1–5 closed or explicitly deferred with maintainer approval
- [ ] Exact release/development versions declared (`1.0.0` / `1.0.0-dev`); no prior-release literal drives automation
- [ ] `make release-preflight` exits 0 and reports steps 1–10 plus all release-file updates
- [ ] `make release-finalize` exits 0 on the unchanged preflight snapshot
- [ ] Maintainer confirms manual steps 11–13 completed successfully (tag `v1.0.0` pushed, RTD `stable` set to `v1.0.0`, PyPI upload done)
- [ ] `make release-postcommit` exits 0 and reports steps 14–17
- [ ] PyPI page/metadata and exact clean-environment install verify `1.0.0`
- [ ] Released plan archived; next maintained plan exists and is content-complete (via `ce-release-planner`)
- [ ] `RELEASE_PLAN_v1.md` itself archived to `development/finished-work/`, closing the v0.6.0→v1.0.0 release series
- [ ] Master release tracking names the released and next milestones correctly
- [ ] `pyproject.toml` and runtime fallback use the declared next development version
- [ ] `make local-checks-task TASK=6` exits 0.

---

## Release gate summary

| Gate criterion | Task | Status |
|---|---|---|
| Staging validation carry-forward confirmed; zero active deprecations | Task 1 | Not started |
| Standard-001/Standard-002 guardrails re-confirmed; maintenance cadence scheduled | Task 2 | Not started |
| GA compatibility statement published (README, CHANGELOG, docs hub) | Task 3 | Not started |
| Documentation hubs current for v1.0.0 (GA notice, install pin, linkcheck, notebook execution re-verified clean, upgrade checklist circulated) | Task 4 | Not started |
| Packaging classifier promoted to `Development Status :: 5 - Production/Stable` | Task 5 | Not started (blocked on Tasks 1–4) |
| Tagged release artifacts available; PyPI/RTD published and verified; plan handoff complete | Task 6 | Not started |

**Not ready to tag `v1.0.0` until Tasks 1–5 are closed and Task 6's pre-tag gate
(`make release-preflight` + `make release-finalize`) passes on the candidate
commit.**

## Minimal new tests required

No new application tests are expected for this milestone (stability
declaration only — no runtime behavior changes). If a release-blocking
defect is found and patched:

- Add the minimum focused test that reproduces the defect and passes after the fix.
- Re-run the affected task gate and Task 6 release-preparation gates.
- Record the defect and its test in the `1.0.0` release notes.

If Task 6 step 6 (`make release-postcommit`) needs `NEXT_VERSION=` exercised
for a non-obvious transition (e.g. `1.0.0` → `1.0.1` vs. `1.0.0` → `1.1.0`),
confirm `tests/scripts/test_local_checks_release_workflow.py` already covers
the version-lineage logic generically (it does not hard-code `1.0.0` or any
prior release number); no new test is required unless a gap is found during
Task 6.

---

## Task verification mappings (machine-readable)

This block is the **source of truth** for `make local-checks-task TASK=<n>`.
`scripts/local_checks.py` is a generic engine: it parses the fenced
`toml ce-task-verification` block below from the active release plan
(discovered from the project version lineage — this plan declares
`**Release version:** 1.0.0` / development version `1.0.0-dev` in its header,
matching the `1.0.0` lineage — overridable with `--plan`) and builds each
task's focused steps and lint targets from it. Without this block,
`make local-checks-task` cannot resolve any task for the active `1.0.0`
lineage, since `_resolved_plan_path` selects this file as the sole
`development/current-work/v*_plan.md` match. Task ids map to this plan's GA
tasks 1–6 (in this plan's execution-order numbering); every step is a
validation/confirmation step, consistent with the GA posture (no planned
implementation).

Step kinds: `pytest = [paths...]` (runner adds `-q`, `-o addopts=`, and
`--no-cov` when available; optional `pytest_args = [...]` are inserted after
the paths) and `command = "..."` (shell-split; a leading `python` resolves to
the active interpreter). `lint_targets` scopes the quick-profile Ruff
check/format steps; if omitted or empty, changed-file detection is used.
The engine fails fast with the supported id set when a task id has no mapping
here, so a `local-checks-task` verification line without a matching mapping
cannot pass.

```toml ce-task-verification
schema_version = 1

[[task.1.steps]]
name = "Task 1 deprecation-closure gate"
command = "python scripts/local_checks.py --deprecation-closure"

[[task.1.steps]]
name = "Task 1 cache smoke tests"
pytest = [
  "tests/unit/cache",
  "tests/unit/perf/test_cache.py",
]

[[task.1.steps]]
name = "Task 1 parallel smoke tests"
pytest = [
  "tests/unit/core/test_parallel_benchmark.py",
  "tests/unit/core/test_parallel_deprecation.py",
  "tests/unit/core/test_parallel_helpers.py",
  "tests/unit/core/explain/test_parallel_executors.py",
  "tests/unit/core/explain/test_parallel_instance_tasks.py",
  "tests/unit/core/explain/test_parallel_lifecycle.py",
  "tests/unit/core/explain/test_parallel_runtime.py",
]

[[task.2.steps]]
name = "Task 2 docstring coverage gate (Standard-002)"
command = "python scripts/quality/check_docstring_coverage.py --fail-under 94.0"

[[task.2.steps]]
name = "Task 2 nomenclature gate (Standard-001)"
command = "python scripts/quality/check_std001_nomenclature.py --root src/calibrated_explanations --check"

[[task.3.steps]]
name = "Task 3 strict docs build"
command = "python -m sphinx -W --keep-going docs docs/_build/html"

[[task.4.steps]]
name = "Task 4 strict docs build"
command = "python -m sphinx -W --keep-going docs docs/_build/html"

[[task.4.steps]]
name = "Task 4 docs linkcheck"
command = "python -m sphinx -b linkcheck docs docs/_build/linkcheck"

[[task.5.steps]]
name = "Task 5 classifier promoted to Production/Stable"
command = "python -c \"import tomllib; d = tomllib.load(open('pyproject.toml','rb')); c = d['project']['classifiers']; assert 'Development Status :: 5 - Production/Stable' in c, c; assert not any(x in c for x in ('Development Status :: 4 - Beta','Development Status :: 3 - Alpha','Development Status :: 6 - Mature')), c; print('OK')\""

[[task.6.steps]]
name = "Task 6 packaging install smoke"
command = "python scripts/local_checks.py --uv-install-smoke"

[[task.6.steps]]
name = "Task 6 warning policy"
command = "python scripts/quality/check_warning_policy.py --check --report reports/quality/warning_policy.json"
```

Task 6's full pre-tag gate is `make release-preflight VERSION=1.0.0` (see
§6.5); the mapping above carries only its fast, standalone spot checks.
