claim_id: CE-CAP-REJECT-001
claim_type: capability
owner: calibrated_explanations
status: current

adr_links:
  - ADR-029
  - ADR-038

claim_text: >
  calibrated_explanations supports reject/defer policies through WrapCalibratedExplainer,
  tagging explanation outputs with rejection status based on calibrated uncertainty scores
  when a RejectPolicySpec is provided.

atomic_rationale: >
  This claim maps to one requirement because the only observable public-API outcome of
  reject policy integration is that the call completes and returns a non-None collection
  of the expected length. The semantic tagging behavior (which instances are flagged vs
  accepted) depends on the calibration set distribution and NCF scoring, which require
  a statistical evaluation study beyond the api_contract scope of this verification pass.
  A separate behavioral requirement for rejection semantics is a documented gap deferred
  to a statistical validation task.

public_api:
  - calibrated_explanations.core.wrap_explainer.WrapCalibratedExplainer.explain_factual
  - calibrated_explanations.core.wrap_explainer.WrapCalibratedExplainer.explore_alternatives
  - calibrated_explanations.RejectPolicySpec

task_types:
  - binary_classification
  - multiclass_classification
  - regression
  - probabilistic_regression

requirements:
  - CE-REQ-REJECT-API-001

verification:
  proves:
    - api_contract
    - output_structure
  verification_type: automated

assumptions:
  - RejectPolicySpec wraps a RejectPolicy enum and an NCF (non-conformity function)
    configuration. The NCF determines how uncertainty is quantified for rejection.
  - The FLAG policy processes all instances and adds rejection tags without filtering.
    ONLY_REJECTED and ONLY_ACCEPTED filter the output to the respective subset.
  - Rejection thresholds are calibrated against the calibration set; coverage of
    reject decisions holds under the exchangeability assumption.
  - This claim does not assert that the reject policy is statistically optimal; it
    asserts that the API is callable and returns a tagged explanation collection.

evidence_required:
  - commit_sha
  - package_version
  - test_id
  - dataset_id
  - random_seed
  - result
