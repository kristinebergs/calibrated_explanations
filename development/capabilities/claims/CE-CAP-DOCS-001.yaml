claim_id: CE-CAP-DOCS-001
claim_type: capability
owner: calibrated_explanations
status: current
adr_links:
  - ADR-012

claim_text: >
  Documentation and gallery builds use maintained source pages and documented build policy rather than obsolete improvement paths.

atomic_rationale: >
  This governance claim maps to one requirement because ADR-012 documentation build policy
  is a single quality gate: source pages exist and build policy is documented. No separate
  behavioral, schema, or runtime requirements are warranted for a documentation governance
  obligation.

public_api: []

task_types:
  - governance

requirements:
  - CE-REQ-DOCS-GOV-001

verification:
  proves:
    - quality_gate
  verification_type: automated_executable_evidence
assumptions:
  - This capability is verified by the executable evidence targets named by its requirement.
  - Metadata linkage checks are drift guards only and do not prove this capability behavior.

evidence_required:
  - commit_sha
  - package_version
  - test_id
  - result
