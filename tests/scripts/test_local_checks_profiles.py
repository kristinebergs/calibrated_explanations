"""Removed by v0.11.6 Task 29 (release-specific local-check meta-tests are prohibited).

This module previously asserted the internal composition of transient release-plan
local-check profiles (step names, task-id mappings, skipped-gate lists, task-report
internals). That is release-plan meta-testing, not a stable CE contract, and it is
now rejected by ``scripts/quality/check_no_release_profile_meta_tests.py``.

The file could not be physically deleted in the authoring session because shell
deletion is denied by the repository permission policy. Delete this stub with::

    git rm tests/scripts/test_local_checks_profiles.py

Do not add tests here.
"""
